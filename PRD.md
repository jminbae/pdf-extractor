# PDF Extractor — 제품 요구사항 (PRD)

> **이 문서가 이 프로젝트의 기준점이다.** 무엇을 왜 만드는지, 무엇이 되면 완성인지가
> 여기 있다. 코드·절차·경위는 아래 「문서 지도」의 각 문서로 간다.
>
> 갱신 2026-07-26 · 저장소 https://github.com/jminbae/pdf-extractor

---

## 1. 문제

의학 논문 PDF에서 **본문을 정확히 뽑는 일**이 안 되고 있다.

원장(피부과 전문의)은 이미 **ResearchMap**(논문 32,758편 라이브 색인, pywebview +
FastAPI + SQLite FTS5)을 배포해 쓰고 있다. 그런데 그 앱의 PDF 추출이 부정확·불완전하다.
ResearchMap PRD §8 한계표에 이렇게 적혀 있다 — **"txt 본문에서 표를 표로 못 뽑음,
그림축 섞임"**. 그 구멍을 메우는 것이 이 프로젝트다.

구체적으로 기존 추출이 놓치는 것:

| 놓치는 것 | 왜 문제인가 |
|---|---|
| **표** | 의학 논문의 결론은 표에 있다. 텍스트로 흘러내리면 셀 관계가 사라진다 |
| **그림** | 임상 사진·forest plot 이 아예 없다 |
| **본문 인용 ↔ 참고문헌 연결** | "효과가 있었다[15]" 에서 15번이 무엇인지 못 간다 |
| **절 구조** | Methods 와 Results 가 섞이면 근거를 특정할 수 없다 |

## 2. 만드는 것

**born-digital 영어 의학 논문 PDF 한 편 → 정본 JSON 한 개 + 그림 PNG 여러 장.**

두 가지 형태로 낸다.

1. **PDF Extractor** — 검수용 데스크탑 앱(Windows). 3분할 화면에서 PDF 원본과
   추출 결과를 나란히 놓고 눈으로 대조한다. 지금의 주 산출물
2. **파이썬 모듈** — ResearchMap 에 얹을 라이브러리. `single.py` 가 진입점

검색 엔진이 아니다. ResearchMap 에 이미 FTS5 전문검색이 있다.

## 3. 사용자와 수용 기준

**사용자는 원장 한 사람**에서 시작해 ResearchMap 사용자 전체로 넓힌다.

> **원장의 수용 기준 (그대로 인용)**
> "167편을 완벽하게 하면 새 100편을 줄게. 거기서도 완벽하면 ResearchMap에 통합하자."

즉 관문이 셋이다. **① 167편 완성 → ② 새 100편 검수 통과 → ③ ResearchMap 통합.**
현재 ①의 후반부에 있다.

"완벽"의 뜻은 규칙 통과가 아니라 **PDF 를 눈으로 대조해 틀린 곳이 없는 것**이다.
이 구분이 이 프로젝트에서 가장 자주 어긋난 지점이다(9장 참고).

## 4. 범위

**한다**

- born-digital **영어** 논문 PDF
- 본문·절 구조·표·그림·참고문헌·서지 메타데이터
- 오프라인 우선. 클라우드 LLM API 비용 0

**하지 않는다**

- 스캔본(텍스트층 없는 PDF), OCR
- **비영어 논문** — 2단 한글 본문은 읽기 순서가 좌우로 섞여
  `피부의 표 여 있는데` 가 되고 그 문단이 제목 자리까지 올라간다(26편 중 5편 실측).
  본문을 비우고 서지정보만 남긴다. 억지로 뽑아 엉터리를 남기지 않는다
- 검색·랭킹·요약 (ResearchMap 몫)
- 특허 문서

## 5. 요구사항

### 5.1 추출 품질 (제품의 본질)

| # | 요구 | 현재 (203편 실측) |
|---|---|---|
| Q1 | 본문이 비어 나오는 논문이 없을 것 | 1편 |
| Q2 | 표를 **표 구조로** 뽑을 것 | 329개 (빈 표 44) |
| Q3 | 그림을 **실제 이미지로** 뽑을 것 | 384개 중 331개(86%) |
| Q4 | 본문 `[15]` → 참고문헌 15번이 맞을 것 | 번호 확정 93% |
| Q5 | 참고문헌 식별자 해소 | DOI 85.4% |
| Q6 | 옆 논문 내용이 섞이지 않을 것 | 합본 34편 중 오탐 0 |
| Q7 | 품질 점수 평균 | 0.863 |

**추출 경로**: GROBID 194 · PyMuPDF 폴백 8 · PMC XML 1.
GROBID 는 선택이 아니라 필수다 — 폴백만 쓰면 품질 0.96→0.65, 표 286→0.

### 5.2 설치와 실행 (2026-07-26 추가)

원장 지시: **"어느 컴퓨터에서도 설치하면 작동해야 한다."**

| # | 요구 | 상태 |
|---|---|---|
| I1 | 설치프로그램 하나만 받으면 될 것 | ✅ 35.8MB, Releases |
| I2 | **관리자 권한 불필요** (병원 PC) | ✅ `PrivilegesRequired=lowest` |
| I3 | 자바·파이썬·Docker 사전 설치 불필요 | ✅ 앱이 엔진을 자동 설치(435MB, 82초) |
| I4 | 백신에 걸리지 않을 것 | ✅ Defender 스캔 통과 |
| I5 | 엔진이 이미 있으면 재사용할 것 | ✅ 위치를 고정하지 않고 탐색 |
| I6 | 엔진을 못 구해도 앱은 뜰 것 | ✅ 폴백으로 진행(품질은 낮다) |
| I7 | 지울 때 뽑아 둔 결과물은 남길 것 | ✅ |

I4 는 실제로 당했다 — `--onefile` exe 를 Defender 가 `Trojan:Win32/Wacatac.H!ml`
로 잡고 **삭제**했다. `--onedir` + Inno Setup 설치프로그램으로 해결.

### 5.3 읽는 경험

잘 뽑아도 읽을 때 어긋나면 사용자에게는 여전히 부정확한 도구다.

| # | 요구 | 근거 |
|---|---|---|
| V1 | 인용 `[15]`·본문 `Figure 1` 은 **그 자리에 팝업**으로 | 아래로 뛰면 읽던 자리를 잃는다. 인용 확인은 "곁눈질"이지 "이동"이 아니다 |
| V2 | 표는 진짜 `<table>` 로 | |
| V3 | 그림은 우클릭으로 복사·저장 | |
| V4 | 이미지에 width/height 를 반드시 넣을 것 | 없으면 로드 전 높이가 0 이라 링크 착지점이 밀린다 |
| V5 | 3분할 1:2:2, 구분선으로 조절 | |
| V6 | 외부 CDN·인터넷 자원 0 | 병원 내부망 |
| V7 | 40~50초 걸리는 엔진 기동에 화면이 얼지 않을 것 | 워커 스레드 |

### 5.4 견고성

| # | 요구 | 왜 |
|---|---|---|
| R1 | 한 편이 실패해도 나머지가 계속될 것 | 실패 원장(`failures.jsonl`) |
| R2 | 중단해도 **이어서** 할 수 있을 것 | 단계별 캐시 |
| R3 | PDF 를 옮기거나 이름을 바꿔도 추출물을 잃지 않을 것 | 짝은 경로가 아니라 **내용 sha1** |
| R4 | 배치 중 GROBID 가 죽으면 되살릴 것 | 실측 175편 중 2회 죽었다 |
| R5 | **로그 한 줄이 프로그램을 죽이지 않을 것** | 실제로 두 번 죽었다(cp949 에 em대시 없음) |
| R6 | 첫 실행에서 엔진을 받는 중이면 기다릴 것 | 정해진 초만 기다리면 **말없이 저품질 산출물**이 나온다 |

### 5.5 성능

처리 시간은 제약이 아니다(분량 비례). 중요한 것은 **재개·격리·비차단**이다.
현재 편당 3.9초(GROBID 동시 2편 제한 — 한 편이 250~400MB 를 쓰고, 몰리면 죽는다).

## 6. 정본 스키마 (계약)

```
paper_id · source · schema_version · quality_score · source_file · meta
· abstract · abstract_source · body_text · figures · tables · references · qc
```

`body_text = [{path[], section_type, paragraphs[{id, text, cited_refs[], cited_keys[],
refs_figure[], refs_table[]}]}]`
`section_type = abstract | intro | methods | results | discussion | back | other`

이름은 S2ORC 관례를 따랐으나 **S2ORC 로 내보내지 않는다** — S2ORC 는 절 계층이 없고,
표 내용이 아예 없으며, 본문에서 인용 마커를 떼어낸다. 셋 다 우리에게 필요하다.

정의: `pubnexus/src/pubnexus/schema.py`

## 7. 확정된 설계 결정 (뒤집지 말 것)

각각 실제로 사고가 나서 정해졌다. 되돌리려면 그때의 실측을 먼저 반박해야 한다.

| 결정 | 이유 |
|---|---|
| **정본은 JSON 하나.** Markdown 은 볼 때 렌더 | md 를 저장하면 JSON 수리 후 조용히 낡아 **두 번째(틀린) 정본**이 된다 |
| **참고문헌 내용을 PDF 에서 파싱하지 않는다.** iCite API 로 조회 | 파싱 참고문헌은 못 믿는다 — DOI 순열 뒤섞임(한 편 125개 중 98개), 이웃 논문 혼입, 펀딩 문구 유입. **원장이 반복 지시한 사항** |
| **번호·순서는 지면, 내용은 iCite → 짝지어 합침** | iCite 순서는 지면 번호가 아니다 |
| **본문 인용은 제자리에 `[15]` 로** | 위첨자가 그냥 뽑히면 `효과가 있었다15,16` 이 되어 본문 수치와 구분이 안 된다 |
| **PDF↔정본 짝은 내용 sha1** | 경로로 맺으면 옮기는 순간 잃는다 |
| **정본 저장은 앱 저장소**(`%LOCALAPPDATA%`) | PDF 폴더를 어지럽히지 않고 읽기 전용 위치에서도 된다 |
| **그림은 조각이 없으면 자르지 않는다** | 없는 것이 엉뚱하게 잘린 것보다 낫다 |
| **틀린 것보다 없는 것이 낫다** | 참고문헌 링크·표 셀 값·잘라낸 그림 전부 이 기준 |
| **GROBID 는 필수** | 파일럿 80%가 GROBID 경로 |
| **`figtab.py` 는 연결하지 않는다** | 같은 지면 **다른 논문의 표**를 가져다 붙이는 오염이 실증됐다(13건 중 4건) |

## 8. 지금 상태 (2026-07-26)

**되는 것**: 0~4.8단계 전 과정, 데스크탑 앱, 윈도우 네이티브 GROBID(Docker 불필요),
엔진 자동 설치, 설치프로그램 배포, GitHub 백업.

**남은 결함** (정직한 목록)

| 결함 | 규모 | 성격 |
|---|---|---|
| 빈 표 | 44개 | 캡션이 조판에서 안 잡히는 24편이 큰 몫 |
| 문단 잘림 | 32편 | GROBID 가 앞부분을 버림 (진짜 손실) |
| 그림 누락 | 22편 | PDF 엔 있는데 못 뽑음 |
| 표 누락 | 14편 | 〃 |
| 전 절 미분류 | 64편 | 대부분 letter·case report — 원래 IMRaD 가 없다 |
| 표에 끊긴 문장 | 49건/38편 | 자동 접합은 안 했다(잘못 이으면 비문) |

**미검증**: 노트북 등 **다른 실제 기계에서의 설치**, Windows 10 + WebView2.

## 9. 일하는 방식 (지켜야 하는 것)

이 프로젝트에서 실제로 하루씩 날린 것들이다.

- **규칙 기반 탐지 ≠ 검증.** 미리 생각해 넣은 결함만 잡힌다. 167편을 눈으로 읽으니
  규칙이 놓친 결함 798건이 나왔다(판정 perfect 15 · minor 37 · broken 113)
- **모든 진입 경로를 실제로 돌려라.** 필드 개명 한 번에 읽기 경로 3개가 동시에
  죽었는데, 캐시로만 확인해서 하루를 날렸다. **GROBID 를 끈 상태로도 돌려 보라**
- **"되는지" 묻거든 그 조건을 실제로 만들어 돌려라.** 새 PC 조건을 재현하고서야
  첫 실행에 저품질로 내려가는 결함을 찾았다
- **남의 기록(내 것 포함)을 믿기 전에 소스를 봐라.** "윈도우 GROBID 불가"라는 어제의
  잘못된 진단 때문에 오늘 우회를 시도하다 헛수고했다
- **수리 전/후를 같은 잣대로 재라.** 한쪽만 새 기준으로 재면 개선처럼 보인다
- **지표를 낮추는 데 최적화하지 마라.** 미분류를 줄이려다 오분류를 성과로 착각했다.
  틀린 라벨보다 정직한 `other` 가 낫다
- **같은 일을 하는 경로가 둘이면 어긋난다.** 배치와 단건에 수리 단계가 다 들어갔는지
  확인하라 — 앱 산출물만 조용히 질이 갈린 적이 있다
- **서브에이전트 보고를 그대로 믿지 말고 직접 재라**

## 10. 로드맵

1. **167편 완성** ← 지금 여기. 빈 표 44개가 가장 큰 덩어리
2. **다른 PC 설치 검증** (노트북, Windows 10)
3. **새 100편 검수** — 원장 수용 기준
4. **ResearchMap 통합** — 저장소 방식(`%LOCALAPPDATA%`)은 이미 맞춰 뒀다

## 11. 제약

| 대상 | 라이선스 | 함의 |
|---|---|---|
| GROBID | Apache-2.0 | 패치·재배포 가능 |
| Temurin JDK | GPLv2 + Classpath Exception | 동봉 배포 가능 |
| **PyMuPDF** | **AGPL-3.0** | **상업 배포 시 문제.** 상용 라이선스 구매 또는 pypdfium2 교체 검토 |
| 논문 PDF | 출판사 저작권 | 추출물 재배포는 별개 문제 |

저장소는 **공개**다 — 앱이 엔진을 인증 없이 내려받아야 해서다(비공개면 404).

## 12. 문서 지도

필요할 때 여기서 갈라진다.

| 무엇이 궁금한가 | 읽을 것 |
|---|---|
| **ResearchMap 에 옮기려면** | `전달용/ResearchMap_이식_가이드.md` — 이 한 파일로 끝나게 썼다(추출 + 화면 표현 + 함정 + 검증법) |
| 파이프라인 단계·모듈 구성 | `pubnexus/README.md` |
| 설치·실행·설정 상세 | `전달용/PubNexus_인수인계_실행가이드.md` |
| 왜 이렇게 됐나 (경위·사고·수리) | `전달용/작업기록_2026-07-26.md` |
| 원 설계 사상 | `전달용/PubNexus_RAG_파이프라인_설계.md` |
| 논문별 대조 감사 결과 | `전달용/감사_167편_2026-07-26.json` |
| GROBID 를 윈도우에서 빌드 | `tools/build_grobid_windows.ps1` (주석에 왜가 다 있다) |
| 엔진 배포본 만들기 | `tools/package_grobid_runtime.ps1` |
| 앱 빌드·설치프로그램 | `build_exe.ps1` · `tools/installer.iss` |

## 13. 용어

| 말 | 뜻 |
|---|---|
| **정본** | 이 파이프라인이 내놓는 단 하나의 진짜 산출물. JSON |
| **폴백** | GROBID 없이 PyMuPDF 만으로 뽑는 저품질 경로. **안전망이지 대안이 아니다** |
| **조각(piece)** | 그림을 이루는 최소 단위 — 내장 이미지 rect 또는 벡터 도형 bbox |
| **합본 지면** | 한 쪽에 여러 편(레터 등)이 이어 실린 지면. 소속 판정은 `boundary.py` |
| **원본 추출** | 지면을 다시 그리지 않고 PDF 안의 이미지 스트림을 그대로 꺼내는 것 |
